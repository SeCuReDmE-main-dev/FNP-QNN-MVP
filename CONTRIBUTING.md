# Contributing

This repository is in an active crafting phase.

Please do not open unsolicited issues, pull requests, pushes, or review requests while the README notice remains present. Local validation work should use:

```bash
python -m unittest discover -s tests -p "test_*.py"
python scripts/validate_alpha_readiness.py
```
